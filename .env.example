# ============================================================
# OpenClass 环境变量配置
# 复制此文件为 .env 并填入实际值
# ============================================================

# 阿里云 AccessKey（通义听悟实时语音识别必需）
# 推荐使用简短环境变量名：ALI_ACCESS_KEY_ID / ALI_ACCESS_KEY_SECRET
# （向后兼容：项目同时支持历史名 ALIBABA_CLOUD_ACCESS_KEY_ID / ALIBABA_CLOUD_ACCESS_KEY_SECRET）
ALI_ACCESS_KEY_ID=YOUR_ACCESS_KEY_ID
ALI_ACCESS_KEY_SECRET=YOUR_ACCESS_KEY_SECRET

# 通义听悟 AppKey（在听悟管控台创建）
TINGWU_APP_KEY=YOUR_TINGWU_APP_KEY

# 大模型配置（默认使用阿里通义QWen）
# 支持: qwen, openai, custom
LLM_PROVIDER=qwen

# 阿里通义千问 API Key (DashScope)
DASHSCOPE_API_KEY=YOUR_DASHSCOPE_API_KEY

# OpenAI API Key (可选，当 LLM_PROVIDER=openai 时使用)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY
OPENAI_BASE_URL=https://api.openai.com/v1

# 自定义模型配置 (可选，当 LLM_PROVIDER=custom 时使用)
CUSTOM_LLM_API_KEY=
CUSTOM_LLM_BASE_URL=
CUSTOM_LLM_MODEL=

# 默认输出语言 (cn, en, ja, ko, de, fr, ru)
OUTPUT_LANGUAGE=cn

# 课堂内容总结间隔（分钟）
SUMMARY_INTERVAL_MINUTES=10

# 课堂数据存储根目录
CLASSROOM_DATA_DIR=./classroom_data
